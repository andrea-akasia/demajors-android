package com.demajors.demajorsapp.util

class Const {
    companion object {
        // prefs
        const val KEY_TOKEN = "TOKEN"
        const val KEY_TOKEN_REFRESH = "TOKEN_REFRESH"
        const val KEY_IS_LOGGED_IN = "IS_LOGGED_IN"
        const val KEY_EMAIL = "EMAIL"

        // Req Code
        const val RC_PERMISSIONS = 100
    }
}
